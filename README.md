# Simulating_a_Simple_Cache_Controller_FSM
This project simulates an FSM-based cache controller with a CPU issuing read/write requests and memory with fixed delay. It demonstrates state transitions (hit, miss, write-back, allocate) and their impact on signals and data flow, supported by test cases and visual explanations.
